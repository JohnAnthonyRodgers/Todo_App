<% content_for :title, "Todos" %>
<h1 class="text-4xl font-bold text-gray-800 mb-6">📝 To-Do List</h1>

<div id="todos">
  <% @todos.each do |todo| %>
    <%= render todo %>
  <% end %>
</div>

<%= link_to "New todo", new_todo_path, class: "inline-block bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-2 px-4 rounded-lg shadow-md mt-6" %>
